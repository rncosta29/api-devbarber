# Nome da aplicacao
spring.application.name=gateway

# URL Servidor de configuracao (devabrber-config-server)
spring.cloud.config.uri=http://localhost:8888

management.endpoints.web.exposure.include=*